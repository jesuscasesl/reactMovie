import React from 'react'

import './Header.css'

const Header = () => (
    <header className = 'header'>
      <h1 className = 'title titleHeard'>React Movie</h1>
    </header>
  )

export default Header
